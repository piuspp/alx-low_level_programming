#include "main.h"
/**
 * _isalpha - function to check if a letter.
 * @c:is the int that will use for argument
 * Return: 0
 */

int _isalpha(int c)
{
	if ((c >= 'a' && v <= 'z') || (c >= 'A' && c <= 'Z'))
	{
	return (1);
	}
	else
	return (0);
}
