#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
i * _abs - function that compute absolute value of an integer
 *  @c: is the int that will use for argument
 *  Return: Always 0 (Success)
 */
int _abs(int c)

{
	int abs_val;
	abs_val = abs(c);
	return abs_val;

}
